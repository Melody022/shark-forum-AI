server:
  port: ${APP_PORT:8081}
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: paicoding-ai

  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/paicoding_ai?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    openai:
      base-url: ${AI_BASE_URL:https://api.deepseek.com}
      api-key: ${AI_API_KEY:}
      chat:
        options:
          model: ${AI_MODEL:deepseek-chat}

ai:
  session:
    title: "HELLO, 我是AI助手"
    describe: "我是由技术派倾力打造的智能助理，我能推荐教程、答疑解惑，还能为您激发创意、畅聊心事。"
    examples:
      - "能帮我推荐一个合适的课吗？"
      - "最近有什么新课吗？"
      - "如何选择适合我的课程？"
      - "Java和Python有什么区别？"
      - "Spring Boot怎么学？"

